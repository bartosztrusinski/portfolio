---
type Props = {
  className?: string;
};

const { className = '' } = Astro.props;
---

<div
  class={`mx-auto grid max-w-[80ch] gap-1 text-pretty px-5 text-justify pb-10 pt-6 ${className}`}>
  <slot />
</div>
